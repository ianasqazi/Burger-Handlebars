

<div class="container-fluid">

    <div class="row">
        <div class="col">
            <div class="text-center">
                <img class="img-fluid" src="/assets/images/burger.png">
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col">
            <div class="text-center">
                <h1>BURGERIZER</h1>
            </div>
        </div>
    </div>
     
</div>

{{!-- ===================================== --}}
<div class="container-fluid">
    <div class="row">
        <div id="devouredBurger" class="col-4 col-md-6">
            <div>
                <h5>DEVOURED BURGERS</h5>
            </div>
            <div>
                <ul>
                    {{#each burgers}}
                        {{#if devoured}}
                            <li>       
                                {{this.burger_name}}
                            </li>
                        {{/if}}
                    {{/each}}
                </ul>
            </div>
        </div>

        <div id="notDevouredBurger" class="col-4 col-md-6">
            <div>
                <h5>NOT DEVOURED BURGERS</h5>
            </div>
            <div>
                <ul>
                    {{#each burgers}}
                        {{#unless devoured}}
                            <li>       
                                {{this.burger_name}}
                                <button data-id="{{this.id}}" class="devourIt btn btn-dark  btn-sm">DEVOUR IT</button>
                            </li>
                        {{/unless}}
                    {{/each}}
                </ul>
            </div>
        </div>
    </div>
</div>

{{!-- ===================================== --}}
<div class="container-fluid" id="addBurgerBlock">

    <div class="row">
        <h2>Add a Burger</h2>
    </div>

    <div class="row">
        <form class="create-form">

        <div class="form-group">
            <label for="burgerName">NAME:</label>
            <input type="text" id="burgerName" name="burgerName">

        <button type="submit" id="newBurger">Add Burger</button>
        </form>
    </div>


</div>


